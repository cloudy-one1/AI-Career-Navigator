# === 环境与密钥 ===
.env
.venv/
venv/

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# === 运行时数据 ===
data/
*.db
*.sqlite3

# === 日志 ===
*.log

# === Node / Vite 前端（v4.0） ===
node_modules/
frontend/dist/

# === 临时测试 / 调试文件 ===
# 约定（v7.2.2 通用化）：仓库根目录 `_` 前缀的文件/目录一律视为临时产物，不入库。
# 历史教训：_commit.py、_tmp_convert/ 曾被误提交（git 历史两次专门清理）——
# 这是反复发作的老毛病，除本规则外配套 tests/test_repo_hygiene.py 在 CI 断言兜底。
/_*
/interview_*.txt
/test_output.txt
/docs/AI对话存档/
# v8.4 整理：实测 .grimp_cache/ 与 .pytest_cache/ 此前均未被覆盖——
# .pytest_cache 靠 pytest 自己在目录内写的 .gitignore 才没进 git status，
# .grimp_cache 同理依赖工具自写规则。依赖关系方自觉不如自己声明，故显式补上。
.grimp_cache/
.pytest_cache/
.import_linter_cache/
.coverage
htmlcov/

# === 对外仓库不含文档（本地保留，不入库）===
# 仓库的公开面是代码本身：README 承担全部对外说明，接口文档由运行时的
# /docs（Swagger UI）自动生成。docs/ 下的过程性资料（需求文档 / 竞品研读 /
# 验收材料等）与下面三份根目录文档，一律**只停止跟踪、不删除**——需要重新入库时，
# 删掉本节对应条目再 git add 即可。
docs/
/CHANGELOG.md
/CHARTER.md
/CODEBUDDY.md

# === 源码打包产物（不入库） ===
*.zip

# === 二进制 / 产品文档（不入库） ===
*.docx
*.doc
*.pdf
*.pptx
*.xlsx

# === IDE / 编辑器 ===
.vscode/
.idea/
*.swp
*.swo
*~

# === CodeBuddy AI 辅助 ===
.codebuddy/
.playwright-cli/

# === 其它 AI 辅助工具（临时扫描脚本与产物） ===
.agnes/
.zcode/

# === [v7.4] Vite / Vitest 缓存（跑 npm run test 会在仓库根生成，非源码）===
.vite/
.vitest/

# === 对话上传附件（不入库） ===
upload_*.jpg
upload_*.png

# === 操作系统 ===
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
.directory
