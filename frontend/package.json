{
  "name": "ai-interviewer-frontend",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "AI 求职陪跑平台 — 前端（Vite + 原生 ES Module）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "chart.js": "^4.4.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
