fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
# <4 上限：SDK 跨 major 的 breaking 不在验证范围内，无上限时"装到什么版本"每天在漂
# （v8.10.1 实测干净环境解析到 3.x 而 dev 是 2.x）。跨 major 升级走 dependabot + 全量验证。
openai>=1.109.1,<4
aiosqlite>=0.22.1
pydantic>=2.13.5
python-multipart>=0.0.9
websockets>=12.0
pdfplumber>=0.11.0
python-docx>=1.1.0
slowapi>=0.1.9
httpx>=0.27.0                 # MiMo 云端语音代理（TTS/ASR）
markdown>=3.5                 # 复盘报告 Markdown→HTML 导出（浏览器打印即 PDF）
pyyaml>=6.0.3                   # 公司风格配置层（company_profiles/*.yaml，缺失时该层降级）

# 实时采集（内嵌 job-crawler 采集器）：需额外执行 playwright install chromium
playwright>=1.63.0
playwright-stealth>=1.0.6

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0

# dev：分层依赖契约检查
import-linter>=2.0
