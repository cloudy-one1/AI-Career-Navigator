# 项目宪章（CHARTER）

> 本文档是 AI 模拟面试官与职业规划项目的**不变宪章**：只记录不随版本变化的根基——产品命题、架构约束、决策记录、诚实局限。版本迭代叙事见 [CHANGELOG.md](CHANGELOG.md)，日常协作入口见 [CODEBUDDY.md](CODEBUDDY.md)。
>
> **变更原则**：本文档的每条内容都应当"少变、定根本"。修改架构约束/产品命题必须补写决策记录卡（见下）。

---

## 产品命题（核心价值）

**全名：AI 模拟面试官与职业规划**。两条产品线，同等地位：

1. **诊断**：评估候选人回答质量（五维度，见架构约束）。
2. **规划**：给定简历 + 目标岗位 + 目标年限，产出**时间轴 + 路径推理**的多阶段发展路径（每阶段需补技能、里程碑、岗位跃迁、推进顺序），而非横截面打分。

**出题—诊断闭环**：出题是诊断的必要前置与质量杠杆——问题围绕诊断维度设计，诊断结果反哺出题策略。我们与题库型/逼真型面试 App 的差异不在"不出题"，而在出题服务于诊断闭环、而非单纯生成逼真题面。

**技术栈**：Python 3.12 / FastAPI + WebSocket / SQLite (aiosqlite) / 多 AI 后端 (DeepSeek/Qwen/智谱/OpenAI) / 原生 ES Module 前端 + Chart.js / logging + RotatingFileHandler (5MB×3 旋转)

---

## 架构约束（必须遵守）

### 约束 1：双 Agent 不可合并

Diagnostician + Rewriter 是两个独立 Agent，**禁止合并为单一 Agent**。诊断和改写必须分步进行，各自有独立的 prompt。论证依据见决策卡 [DC-01](#决策记录卡)。

### 约束 2：分层依赖约束

目录不作为强制的物理分层，但导入方向必须遵守**逻辑分层**。下半层禁止 import 上半层：

| 层级 | 模块 | 允许依赖 |
|---|---|---|
| L1 基础设施 | `config.py` `logger.py` `llm_client.py` `db.py` | 仅标准库 / 三方库，**无项目内 import**（config 除外） |
| L2 领域模型/数据 | `schemas.py` `security.py` `resume_parser.py` `dimension_weights.py` `gap_analyzer.py` `market/*` | 仅 L1（`from .config import ...` `from .db import ...`） |
| L3 业务逻辑 | `question_gen.py` `diagnosis_engine.py` `interview_engine/*` `web_research.py` `question_bank.py` `data_support.py` `career_planner.py` | L1 + L2，禁止 import L4 |
| L4 应用入口 | `main.py` | 所有层 |

> 说明：`market/*` 从 v3.1 的 L3 调整为 L2，因为 `gap_analyzer`（L2）实际已依赖 `market`（v3.1 市场基准注入），若不调整即构成 L2→L3 越层。分层按"代码现状 + 语义（数据访问）"双校准。

**反模式**（必须避免）：
- L2 模块 `from .main import ...` — 绝对禁止
- L3 模块 `from backend.config import ...` — 应改为相对 import `from .config import ...`
- 循环 import：`session.py` ↔ `main.py` 通过函数参数注入打破循环（v2.6 踩过此坑）

**强制检查**（v3.2 起，取代"随机抽查"）：`.importlinter` 契约文件 + `python run.py lint` 一键执行（等价于 `PYTHONUTF8=1 python -m importlinter.cli lint`；注意裸 `python -m importlinter` 只打印帮助不执行检查，Windows 下不设 PYTHONUTF8 会导致 grimp 按 GBK 解析 UTF-8 源码而误判）。任何新增/重构模块后必须跑通契约。

### 约束 3：诊断五维度

诊断始终围绕五个维度评分：
1. STAR 完整性
2. 量化程度
3. 逻辑连贯性
4. 岗位相关性
5. 专业深度

维度**数量**由本宪章定义，不可增减；`dimension_weights.py` 只允许调整各维度的**权重**（见 CHANGELOG v2.6）。

---

## 决策记录卡

> **强制模板**：每次做出不可逆或有争议的架构/范围决策，必须按以下格式记录一张卡片。该机制替代"事后从对话提炼批判性思维信号"的注水方式——格式固定、含反事实对比，真实性可验证、答辩可脱稿复述。

```markdown
### DC-XXX 决策标题
- **日期**：YYYY-MM-DD
- **决策**：是什么
- **放弃的替代方案**：(至少一项)
- **判断依据**：(工程原则/数据/约束，非感受)
- **若错代价**：(错在何处、代价多大、如何发现、何时推翻)
```

### DC-01 双 Agent 不可合并（2026-08-13 补录论证）

- **决策**：Diagnostician 与 Rewriter 保持两个独立 Agent，禁止合并为单一 Agent。
- **放弃的替代方案**：单 Agent 一次调用同时输出诊断 + 改写。
- **判断依据**：诊断是"分析"任务（评分 + 找出问题），改写是"生成"任务（基于诊断重写）；两者角色、system prompt、输出 schema 不同，合并会使"评分客观性"与"改写质量"耦合——同一模型若已知自己要改写，可能为产出漂亮改写而调高评分（自利偏差）。分离保证诊断先于改写、评分不受改写意图污染。
- **若错代价**：若合并不损害评分客观性且大幅降低成本（省一次调用），则此约束是过度设计。代价指标：双 Agent 每题 ≥2 次 LLM 调用、流式延迟与 token 成本翻倍（已披露为已知局限）。何时推翻：若出现"合并后诊断质量与拆分版本无显著差异"的 A/B 证据，或成本成为不可承受瓶颈，应重新评估。

### DC-02 推翻"不复用约束"（2026-08，v3.0）

- **决策**：允许复用 job-crawler 的 `data.db` 数据资产导入 `market.db`，不再用 Playwright 自行采集；移除 playwright 依赖（~200MB）。
- **放弃的替代方案**：维持原红线"不能复用 job-crawler data.db"，继续自行采集。
- **判断依据**：导入数据资产 ≠ 复现代码；Playwright 重复采集纯粹浪费算力与时间（已由用户确认授权）。
- **若错代价**：该决策被事后评估为"遇到重新采集太麻烦后找技术理由绕过既往红线"，且"数据资产≠代码复现"的论证站不住——若评审基于"本周期实际产出"而非"管道整合"，此部分可辩护性较弱。已主动披露（见已知局限-市场基准数据来源）。**何时推翻**：无法推翻（已落地），但答辩叙事须诚实披露而非辩护。

### DC-03 补建职业规划功能线（2026-08-13，v3.2）

- **决策**：项目全名后半段"与职业规划"长期未落地，本轮补建真正的时间轴路径规划模块（`career_planner.py`，L3）。
- **放弃的替代方案**：(a) 仅修命名——从产品名移除"与职业规划"；(b) 仅文档披露缺口而不实现。
- **判断依据**：证据（全文搜索 `职业规划|career` 仅命中出题提示词两处 + 无任何模块产出路径规划）表明这是需求/命名阶段的**遗漏**而非有意砍掉——若为有意砍掉应有范围收窄决策记录。产品命题的一半未被回应，属系统性缺口，比具体 bug 更根本。用户 q-career 明确选择"补建规划功能"而非改名/披露。
- **若错代价**：若时间轴路径推理质量不佳（LLM 多步推理稳定性未经 A/B 验证），则该功能线沦为"换皮 Gap 分析"。发现方式：测试套件仅验证工程正确性，规划质量依赖 LLM，须在答辩中诚实披露此局限。何时推翻：无。

---

## 已知局限（诚实披露）

> 本系统定位为**课程项目**。以下局限是已知且刻意的取舍，不是待修复的 bug；自 2026-08 起，README 与本文档不再使用"生产级加固""5 层安全体系"等夸大定性。

- **无认证/授权**：`session_id` 为随机串，防猜测但**不是访问控制**；拿到链接可读他人简历/诊断/报告。认证属"未来优化方向（课程项目阶段不做）"。
- **WebSocket 无连接身份校验（2026-08）**：`/ws/interview/{session_id}` 仅校验会话是否存在于 `active_sessions`，不校验连接者身份；任何持有 session_id 的客户端均可接管该会话流程，与"无认证/授权"同源。根因是系统无认证层，非单点代码缺陷。v3.1 已为 `switch_provider` 的全局单例重赋值加 `asyncio.Lock` 消除重赋值竞态，但**运行中多会话共用同一全局 `llm_client`/`diagnosis_engine` 实例、无按会话隔离**仍属已知架构局限，不在此阶段引入认证/隔离。
- **全局单例可变状态（2026-08）**：`llm_client` 与 `diagnosis_engine` 为模块级单例；`switch_provider` 重赋值已加锁，但运行中这些单例被所有会话共享、无按会话隔离，多后端高频切换或并发导入场景下内部 provider 状态存在理论竞态。该局限与"WebSocket 无身份校验"同源（无认证/多租户隔离层），课程项目阶段不做隔离，仅以文档披露。
- **双 Agent 成本**：Diagnostician + Rewriter 每题至少 2 次 LLM 调用，流式下延迟与 token 成本翻倍；"优于单 Agent + 结构化输出"缺乏量化对比实验，属先验架构偏好而非被验证的选择（见 DC-01）。
- **SQLite 扩展性**：单文件库；多 Worker 下 WebSocket 需 sticky session，水平扩展受限。
- **内容护栏可被绕过**：正则过滤防不住认真攻击者（换说法/错别字/同义/中英混杂/编码均可绕过）。
- **测试偏纯函数**：50+ 用例覆盖权重计算/Schema 校验等确定性逻辑；但诊断准确性、追问是否抓最弱维度、评分稳定性依赖 LLM 输出，单测难以验证。"用例通过"≠"核心功能被验证"——核心诊断质量依赖 LLM，当前套件验证的是**工程正确性**而非**诊断有效性**。
- **市场基准数据来源（学术诚信披露，2026-08）**：Gap 分析的"市场基准参照"数据来自本人此前已完成并提交的采集项目（job-crawler）的 `data.db`，经 `market/importer` 字段映射 + `store` upsert 导入 `market.db`，本次**仅做管道整合、不含数据采集工作量**。"数据资产≠代码复现"的论证站不住——若评审基于"本次周期实际产出了什么"，此部分可辩护性较弱，已主动披露。决策反转（v3.0"推翻不复用约束"）未做充分利弊审视，属遇到"重新用 Playwright 采集太麻烦"后找技术理由绕过既往红线的取舍；由用户本人负责定夺，AI 仅记录（见 DC-02）。
- **权重注入 prompt 因果未验证（2026-08）**：Diagnostician prompt 中嵌入"维度权重"描述，但权重真正生效处在 `weighted_score()` 的纯数学加权平均；prompt 是否改变模型打分分布未经 A/B 实验验证。现已将 prompt 措辞改为中性（"权重仅用于加权总分，请一致评分"），避免制造"权重已影响评分"的假象——即承认此前那句"评分更审慎"属无因果依据的噪声。
- **LLM 权重稳定性未测（2026-08）**：`dimension_weights.analyze_jd_weights()` 用 LLM 判 JD 权重，`temperature=0.2` 非完全确定；"千岗千面动态评估"卖点的权重在同 JD 下的方差从未测过，稳定性无可辩护证据。
- **输出检测仅监控不阻断（2026-08）**：`security.check_output()` 检测到 Prompt 片段泄漏仅 `logger.warning`，调用方只记日志、不拦截、不脱敏，泄漏内容原样返回前端——纯可观测性、非输出安全边界；请勿在答辩中描述成"防护层"。此外输入正则已收敛纯英文句式（中文面试场景命中率近零）。
- **职业规划路径推理稳定性未验证（2026-08，v3.2）**：`career_planner` 的时间轴路径依赖 LLM 多步推理，`temperature` 非完全确定；同简历同岗位的规划路径方差从未测过，多阶段路径的"顺序合理性"缺乏客观评估标准，可能产出看似完整但无实操依据的路径。测试套件仅验证 schema 与路由工程正确性，不验证路径质量。

---

## 范围纪律

- **禁止**在未经用户明确批准的情况下新增功能模块
- 如认为某功能确有必要，只能以建议形式提出并等待用户批准，不得直接实现
- 已批准范围按版本记录在 [CHANGELOG.md](CHANGELOG.md)（"范围纪律"清单）
- **未来优化方向（课程项目阶段不做）**：用户认证系统、语音升级（云端 TTS/STT）、HTTPS / 局域网安全访问。三者经第一性原理评估均不提升诊断内核质量，属产品化 / 上云阶段事项。

---

## 开发纪律

### 变更前复述
用户指出问题后，须先复述问题本质，确认理解无误后再给出修改方案，不得跳过复述直接改代码。

### Commit Message 规范
禁止使用"fix bug"、"优化代码"等空泛描述，必须写清楚：
```
改了什么: ...
为什么改: ...
```

### 模块开发前
每个功能模块开始前，先输出需求理解文档，存入：
```
docs/weekN_模块名_需求.md
```
内容包含：模块目标、技术方案、涉及的知识点。

### 修改迭代时
用户提出修改意见后，在对应需求文档里追加一段：
```
## 修改记录 [日期]
- 原方案：...
- 用户指出的问题：...
- 修改后的方案：...
```
这是评分中"批判性思维"分项的核心证据，必须真实、具体，不能用"优化了逻辑"这种空泛表述。写入时必须显式写出**原方案的问题本质**与**用户批判性判断点**（依据/工程原则/取舍理由），而不只是复述指令。

### 推送前必做
用户说"推送更改"时，**默认**按以下顺序执行，无需再次确认：
1. **更新 README.md**：同步本轮改动（新增功能、修复记录、项目结构变化、工程改动等）。
2. **更新 CHANGELOG.md**：若本轮有版本迭代，追加对应条目。
3. **更新 CHARTER.md**：若改动涉及架构约束、产品命题、决策记录卡，同步更新。
4. **推送到远端**：完成上述文档更新后再 `git push`。

> 例外：若用户当次明确说"只推代码/不要动文档"，则跳过文档更新步骤。

### 项目收尾
项目开发结束前，自动汇总所有需求文档和周报，按时间线整理成一份完整的《AI协作过程记录》：
```
docs/AI协作过程记录_完整版.md
```
汇总内容须包含独立章节 **"批判性思维轨迹"**，按时间线梳理用户的关键质疑、审计动作与决策反转。

---

## 边界声明

以下事项由用户本人负责，AI 只记录决策结果：
- 诊断维度与规划路径的设计取舍
- 是否开发数据支撑模块
- 项目范围的增减
- 答辩叙事逻辑

---

## 开工自查清单

在新对话/会话中接手本项目时，开始工作前请确认：
1. 是否已阅读 [CHANGELOG.md](CHANGELOG.md) 了解当前版本状态
2. 是否清楚当前处于第几周、上一次的修改记录是什么
3. 是否遵守本宪章的架构约束（尤其 .importlinter 契约）与开发纪律
